[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python server.py & python main_scraper.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
